Step 1. install m4: $sudo apt-get install m4
Step 2. download GMP, uncompress and cd into the directory gmp-XXX
        On the command line:
        $cd ... ./gmp-6.1.2
        $./configure --prefix=/usr/local/gmp-6.1.2  (这里是指定安装目录)
        $make
        //$make check(need much time to run this)
        $make install
        --------------------------------------------------------------
        $sudo make install
        --------------------------------------------------------------
        $./configure --enable-cxx --prefix=/usr/local/gmp-6.1.2 CPPFLAGS=-fexceptions
        --------------------------------------------------------------
        fatal error:'gmp.h' file not found
        $sudo apt-get install libgmp3-dev
        
        This should install GMP into /usr/local
        
